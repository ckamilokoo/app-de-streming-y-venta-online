# Mocks: 1 = sin cuentas externas (dev local). 0 = servicios reales.
CF_MOCK=1
AUTH_MOCK=1

# Cloudflare Stream (CF_MOCK=0)
CF_ACCOUNT_ID=
CF_STREAM_API_TOKEN=

# Clerk (AUTH_MOCK=0)
CLERK_SECRET_KEY=
CLERK_JWKS_URL=
CLERK_ISSUER=

# App
DB_PATH=data/app.db
UPLOADS_DIR=data/uploads
CORS_ORIGINS=http://localhost:3000
